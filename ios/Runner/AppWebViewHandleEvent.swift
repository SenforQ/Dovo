
//: Declare String Begin

/*: "typeName" :*/
fileprivate let appRepresentPath:[UInt8] = [0x65,0x6d,0x61,0x4e,0x65,0x70,0x79,0x74]

/*: "deviceID" :*/
fileprivate let const_documentTitle:[UInt8] = [0x3f,0x3e,0x2d,0x32,0x38,0x3e,0x12,0x1f]

private func largeNew(disk num: UInt8) -> UInt8 {
    return num ^ 91
}

/*: "fireBaseID" :*/
fileprivate let dataAccessName:[UInt8] = [0x81,0x84,0x8d,0x80,0x5d,0x7c,0x8e,0x80,0x64,0x5f]

fileprivate func paperPanel(info num: UInt8) -> UInt8 {
    let value = Int(num) + 229
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "areaISO" :*/
fileprivate let userFoundationTempStr:[UInt8] = [0x4f,0x53,0x49,0x61,0x65,0x72,0x61]

/*: "isProxy" :*/
fileprivate let showNowName:[UInt8] = [0x79,0x78,0x6f,0x72,0x50,0x73,0x69]

/*: "langCode" :*/
fileprivate let userCornerId:[UInt8] = [0x43,0x4e,0x41,0x48,0x6c,0x40,0x4b,0x4a]

private func okOption(system num: UInt8) -> UInt8 {
    return num ^ 47
}

/*: "timeZone" :*/
fileprivate let const_startUrl:[UInt8] = [0x9d,0x80,0x84,0x8c,0xb3,0x86,0x87,0x8c]

private func indexFull(name num: UInt8) -> UInt8 {
    return num ^ 233
}

/*: "installedApps" :*/
fileprivate let notiRunValue:[UInt8] = [0x84,0x89,0x8e,0x8f,0x7c,0x87,0x87,0x80,0x7f,0x5c,0x8b,0x8b,0x8e]

fileprivate func achromaticColor(decision num: UInt8) -> UInt8 {
    let value = Int(num) - 27
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "systemUUID" :*/
fileprivate let userInputContent:[UInt8] = [0x18,0x12,0x18,0x1f,0xe,0x6,0x3e,0x3e,0x22,0x2f]

private func tagAdjust(click num: UInt8) -> UInt8 {
    return num ^ 107
}

/*: "countryCode" :*/
fileprivate let userTapMsg:[UInt8] = [0x65,0x64,0x6f,0x43,0x79,0x72,0x74,0x6e,0x75,0x6f,0x63]

/*: "status" :*/
fileprivate let showThatData:[UInt8] = [0x89,0x8e,0x9b,0x8e,0x8f,0x89]

/*: "isAuth" :*/
fileprivate let main_allowInputKey:[UInt8] = [0x24,0x3e,0xc,0x38,0x39,0x25]

private func cameraResult(selected num: UInt8) -> UInt8 {
    return num ^ 77
}

/*: "isFirst" :*/
fileprivate let userMicMessage:[UInt8] = [0x70,0x7a,0x4d,0x70,0x79,0x7a,0x7b]

fileprivate func fieldLevel(result num: UInt8) -> UInt8 {
    let value = Int(num) + 249
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: __LocalPush" :*/
fileprivate let appFatalUrl:String = "__Locpost register short scheme that"
fileprivate let notiResponseData:String = "alPushlevel failure"

/*: "identifier" :*/
fileprivate let dataSinceName:[UInt8] = [0xd3,0xce,0xcf,0xd8,0xde,0xd3,0xd0,0xd3,0xcf,0xdc]

fileprivate func upMain(create num: UInt8) -> UInt8 {
    let value = Int(num) - 106
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "HTTPProxy" :*/
fileprivate let show_sceneRevenueObserveId:[Character] = ["H","T","T","P","P","r","o"]
fileprivate let mainUpId:String = "temp"

/*: "HTTPSProxy" :*/
fileprivate let main_canStr:String = "revenueTTPSP"
fileprivate let show_sizeStr:String = "actuallyxy"

/*: "SOCKSProxy" :*/
fileprivate let userPoorId:String = "socks"
fileprivate let main_pathName:String = "Proxysub map zone load where"

/*: "__SCOPED__" :*/
fileprivate let show_numberPath:[Character] = ["_","_","S","C","O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let constStopMsg:String = "tcenterp"

/*: "tun" :*/
fileprivate let notiProductionNowWindowPath:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let notiWhiteMessage:String = "ipsfeedbackc"

/*: "ppp" :*/
fileprivate let app_confirmClickKey:[Character] = ["p","p","p"]

/*: "Retry After or Go to " :*/
fileprivate let noti_statusText:String = "Retrfatal revenue at"
fileprivate let show_presentationAdjustPendingText:String = "er or Gosuper arrow production"
fileprivate let appMakeKey:String = " to head white normal"

/*: "Feedback" :*/
fileprivate let app_sceneId:String = "sound enter deadlineFeedback"

/*: " to contact us" :*/
fileprivate let main_firstUrl:[Character] = [" ","t","o"]
fileprivate let notiTransactionValue:[Character] = [" ","c","o","n","t","a","c","t"," ","u","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AppWebViewHandleEvent.swift
//  OverseaH5
//
//  Created by young on 2025/9/23.
//

//: import CoreTelephony
import CoreTelephony
//: import FirebaseMessaging
import FirebaseMessaging
//: import HandyJSON
import HandyJSON
//: import StoreKit
import StoreKit
//: import UIKit
import UIKit

//: enum TypeName: String, HandyJSONEnum {
enum SubComparable: String, HandyJSONEnum {
    //: case unDefaultMethod = "unDefaultMethod"    // 未定义方法
    case unDefaultMethod // 未定义方法
    //: case getDeviceID     = "getDeviceID"        // 获取设备号
    case getDeviceID // 获取设备号
    //: case getFireBaseID   = "getFirebaseID"      // 获取FireBaseToken
    case getFireBaseID = "getFirebaseID" // 获取FireBaseToken
    //: case getAreaISO      = "getAreaISO"         // 获取 SIM/运营商 地区ISO
    case getAreaISO // 获取 SIM/运营商 地区ISO
    //: case getProxyStatus  = "getProxyStatus"     // 获取是否使用了代理
    case getProxyStatus // 获取是否使用了代理
    //: case getMicStatus    = "getMicStatus"       // 获取麦克风权限
    case getMicStatus // 获取麦克风权限
    //: case getPhotoStatus  = "getPhotoStatus"     // 获取相册权限
    case getPhotoStatus // 获取相册权限
    //: case getCameraStatus = "getCameraStatus"    // 获取相机权限
    case getCameraStatus // 获取相机权限
    //: case reportAdjust    = "reportAdjust"       // 上报Adjust
    case reportAdjust // 上报Adjust
    //: case requestLocalPush = "requestLocalPush"  // APP在后台收到音视频通话推送
    case requestLocalPush // APP在后台收到音视频通话推送
    // 新增协议
    //: case getLangCode      = "getLangCode"        // 获取系统语言
    case getLangCode // 获取系统语言
    //: case getTimeZone      = "getTimeZone"        // 获取当前系统时区
    case getTimeZone // 获取当前系统时区
    //: case getInstalledApps = "getInstalledApps"   // 获取已安装应用列表
    case getInstalledApps // 获取已安装应用列表
    //: case getSystemUUID    = "getSystemUUID"      // 获取系统UUID
    case getSystemUUID // 获取系统UUID
    //: case getCountryCode   = "getCountryCode"     // 获取当前系统地区
    case getCountryCode // 获取当前系统地区
    //: case inAppRating      = "inAppRating"        // 应用内评分
    case inAppRating // 应用内评分
    //: case apPay            = "apPay"              // 苹果支付
    case apPay // 苹果支付
    //: case subscribe        = "subscribe"          // 苹果支付-订阅
    case subscribe // 苹果支付-订阅
    //: case openSystemBrowser = "openSystemBrowser" // 调用系统浏览器打开url
    case openSystemBrowser // 调用系统浏览器打开url
    //: case closeWebview     = "closeWebview"       // 关闭当前webview
    case closeWebview // 关闭当前webview
    //: case openNewWebview   = "openNewWebview"     // 使用新webview打开url
    case openNewWebview // 使用新webview打开url
    //: case reloadWebview    = "reloadWebview"      // 重载webView
    case reloadWebview // 重载webView
}

//: struct JSMessageModel: HandyJSON {
struct ThresholdMeasurable: HandyJSON {
    //: var typeName: TypeName = .unDefaultMethod
    var typeName: SubComparable = .unDefaultMethod
    //: var token: String?
    var token: String?
    //: var totalCount: Double?
    var totalCount: Double?

    //: var showText: String?
    var showText: String?
    //: var data: UserInfoModel?
    var data: SharedHandyJSON?
    // 内购/订阅 H5参数
    //: var goodsId: String?
    var goodsId: String? // 商品Id
    //: var source: Int?
    var source: Int? // 充值来源
    //: var type: Int?
    var type: Int? // 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    //: var url: String?
    var url: String? // url
    //: var fullscreen: Int?
    var fullscreen: Int? // fullscreen：0:页面从状态栏以下渲染 1:全屏
    //: var transparency: Int?
    var transparency: Int? // transparency：0-webview白色背景 1-webview透明背景
}

//: struct UserInfoModel: HandyJSON {
struct SharedHandyJSON: HandyJSON {
    //: var headPic: String?
    var headPic: String? // 头像
    //: var nickname: String?
    var nickname: String? // 昵称
    //: var uid: String?
    var uid: String? // 用户Id
}

//: extension AppWebViewController {
extension ChangeRunningViewController {
    //: func handleH5Message(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
    func global(schemeDic: [String: Any], callBack: @escaping (_ backDic: [String: Any]) -> Void) {
        //: if let model = JSMessageModel.deserialize(from: schemeDic) {
        if let model = ThresholdMeasurable.deserialize(from: schemeDic) {
            //: switch model.typeName {
            switch model.typeName {
            //: case .getDeviceID:
            case .getDeviceID:
                //: let adidStr = AppAdjustManager.getAdjustAdid()
                let adidStr = GrantAdjustManager.comprehendible()
                //: callBack(["typeName": model.typeName.rawValue, "deviceID": adidStr])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: const_documentTitle.map{largeNew(disk: $0)}, encoding: .utf8)!: adidStr])

            //: case .getFireBaseID:
            case .getFireBaseID:
                //: AppWebViewController.getFireBaseToken { str in
                ChangeRunningViewController.appear { str in
                    //: callBack(["typeName": model.typeName.rawValue, "fireBaseID": str])
                    callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: dataAccessName.map{paperPanel(info: $0)}, encoding: .utf8)!: str])
                }

            //: case .getAreaISO:
            case .getAreaISO:
                //: let arr = AppWebViewController.getCountryISOCode()
                let arr = ChangeRunningViewController.can()
                //: callBack(["typeName": model.typeName.rawValue, "areaISO": arr.joined(separator: ",")])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: userFoundationTempStr.reversed(), encoding: .utf8)!: arr.joined(separator: ",")])

            //: case .getProxyStatus:
            case .getProxyStatus:
                //: let status = AppWebViewController.getUsedProxyStatus()
                let status = ChangeRunningViewController.launch()
                //: callBack(["typeName": model.typeName.rawValue, "isProxy": status])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: showNowName.reversed(), encoding: .utf8)!: status])

            //: case .getLangCode:
            case .getLangCode:
                //: let langCode = UIDevice.langCode
                let langCode = UIDevice.langCode
                //: callBack(["typeName": model.typeName.rawValue, "langCode": langCode])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: userCornerId.map{okOption(system: $0)}, encoding: .utf8)!: langCode])

            //: case .getTimeZone:
            case .getTimeZone:
                //: let timeZone = UIDevice.timeZone
                let timeZone = UIDevice.timeZone
                //: callBack(["typeName": model.typeName.rawValue, "timeZone": timeZone])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: const_startUrl.map{indexFull(name: $0)}, encoding: .utf8)!: timeZone])

            //: case .getInstalledApps:
            case .getInstalledApps:
                //: let apps = UIDevice.getInstalledApps
                let apps = UIDevice.getInstalledApps
                //: callBack(["typeName": model.typeName.rawValue, "installedApps": apps])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: notiRunValue.map{achromaticColor(decision: $0)}, encoding: .utf8)!: apps])

            //: case .getSystemUUID:
            case .getSystemUUID:
                //: let uuid = UIDevice.systemUUID
                let uuid = UIDevice.systemUUID
                //: callBack(["typeName": model.typeName.rawValue, "systemUUID": uuid])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: userInputContent.map{tagAdjust(click: $0)}, encoding: .utf8)!: uuid])

            //: case .getCountryCode:
            case .getCountryCode:
                //: let countryCode = UIDevice.countryCode
                let countryCode = UIDevice.countryCode
                //: callBack(["typeName": model.typeName.rawValue, "countryCode": countryCode])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: userTapMsg.reversed(), encoding: .utf8)!: countryCode])

            //: case .inAppRating:
            case .inAppRating:
                //: callBack(["typeName": model.typeName.rawValue])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue])
                //: AppWebViewController.requestInAppRating()
                ChangeRunningViewController.currencySource()

            //: case .apPay:
            case .apPay:
                //: if let goodsId = model.goodsId, let source = model.source {
                if let goodsId = model.goodsId, let source = model.source {
                    //: self.applePay(productId: goodsId, source: source, payType: .Pay) { success in
                    self.businessLine(productId: goodsId, source: source, payType: .Pay) { success in
                        //: callBack(["typeName": model.typeName.rawValue, "status": success])
                        callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: showThatData.map{$0^250}, encoding: .utf8)!: success])
                    }
                }

            //: case .subscribe:
            case .subscribe:
                //: if let goodsId = model.goodsId {
                if let goodsId = model.goodsId {
                    //: self.applePay(productId: goodsId, payType: .Subscribe) { success in
                    self.businessLine(productId: goodsId, payType: .Subscribe) { success in
                        //: callBack(["typeName": model.typeName.rawValue, "status": success])
                        callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: showThatData.map{$0^250}, encoding: .utf8)!: success])
                    }
                }

            //: case .openSystemBrowser:
            case .openSystemBrowser:
                //: callBack(["typeName": model.typeName.rawValue])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue])
                //: if let urlStr = model.url, let openURL = URL(string: urlStr) {
                if let urlStr = model.url, let openURL = URL(string: urlStr) {
                    //: UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                    UIApplication.shared.open(openURL, options: [:], completionHandler: nil)
                }

            //: case .closeWebview:
            case .closeWebview:
                //: callBack(["typeName": model.typeName.rawValue])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue])
                //: self.closeWeb()
                self.group()

            //: case .openNewWebview:
            case .openNewWebview:
                //: callBack(["typeName": model.typeName.rawValue])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue])
                //: if let urlStr = model.url,
                if let urlStr = model.url,
                   //: let transparency = model.transparency,
                   let transparency = model.transparency,
                   //: let fullscreen = model.fullscreen {
                   let fullscreen = model.fullscreen
                {
                    //: AppWebViewController.openNewWebView(urlStr, transparency, fullscreen)
                    ChangeRunningViewController.messageDown(urlStr, transparency, fullscreen)
                }

            //: case .reloadWebview:
            case .reloadWebview:
                //: callBack(["typeName": model.typeName.rawValue])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue])
                //: self.reloadWebView()
                self.panel()

            //: case .getMicStatus:
            case .getMicStatus:
                //: AppPermissionTool.shared.requestMicPermission { auth, isFirst in
                SizePermissionTool.shared.mic { auth, isFirst in
                    //: callBack(["typeName": model.typeName.rawValue, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: main_allowInputKey.map{cameraResult(selected: $0)}, encoding: .utf8)!: auth, String(bytes: userMicMessage.map{fieldLevel(result: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case .getPhotoStatus:
            case .getPhotoStatus:
                //: AppPermissionTool.shared.requestPhotoPermission { auth, isFirst in
                SizePermissionTool.shared.begin { auth, isFirst in
                    //: callBack(["typeName": model.typeName.rawValue, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: main_allowInputKey.map{cameraResult(selected: $0)}, encoding: .utf8)!: auth, String(bytes: userMicMessage.map{fieldLevel(result: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case .getCameraStatus:
            case .getCameraStatus:
                //: AppPermissionTool.shared.requestCameraPermission { auth, isFirst in
                SizePermissionTool.shared.block { auth, isFirst in
                    //: callBack(["typeName": model.typeName.rawValue, "isAuth": auth, "isFirst": isFirst])
                    callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue, String(bytes: main_allowInputKey.map{cameraResult(selected: $0)}, encoding: .utf8)!: auth, String(bytes: userMicMessage.map{fieldLevel(result: $0)}, encoding: .utf8)!: isFirst])
                }

            //: case .reportAdjust:
            case .reportAdjust:
                //: if let token = model.token {
                if let token = model.token {
                    //: if let count = model.totalCount {
                    if let count = model.totalCount {
                        //: AppAdjustManager.addPurchasedEvent(token: token, count: count)
                        GrantAdjustManager.everyPost(token: token, count: count)
                        //: } else {
                    } else {
                        //: AppAdjustManager.addEvent(token: token)
                        GrantAdjustManager.dismiss(token: token)
                    }
                }
                //: callBack(["typeName": model.typeName.rawValue])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue])

            //: case .requestLocalPush:
            case .requestLocalPush:
                //: callBack(["typeName": model.typeName.rawValue])
                callBack([String(bytes: appRepresentPath.reversed(), encoding: .utf8)!: model.typeName.rawValue])
                //: AppWebViewController.pushLocalNotification(model)
                ChangeRunningViewController.field(model)

            //: default: break
            default: break
            }
        }
    }
}

// MARK: - Event

//: extension AppWebViewController {
extension ChangeRunningViewController {
    /// 打开新的webview
    /// - Parameters:
    ///   - urlStr: web地址
    ///   - transparency: 0：白色背景 1：透明背景
    ///   - fullscreen: 0:页面从状态栏以下渲染  1：全屏
    //: class func openNewWebView(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
    class func messageDown(_ urlStr: String, _ transparency: Int = 0, _ fullscreen: Int = 1) {
        //: let vc = AppWebViewController()
        let vc = ChangeRunningViewController()
        //: vc.urlString = urlStr
        vc.urlString = urlStr
        //: vc.clearBgColor = (transparency == 1)
        vc.clearBgColor = (transparency == 1)
        //: vc.fullscreen = (fullscreen == 1)
        vc.fullscreen = (fullscreen == 1)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: AppConfig.currentViewController()?.present(vc, animated: true)
        CutAppConfig.futurism()?.present(vc, animated: true)
    }

    /// 本地推送
    //: class func pushLocalNotification(_ model: JSMessageModel) {
    class func field(_ model: ThresholdMeasurable) {
        //: guard UIApplication.shared.applicationState != .active else { return }
        guard UIApplication.shared.applicationState != .active else { return }
        //: UNUserNotificationCenter.current().getNotificationSettings { setting in
        UNUserNotificationCenter.current().getNotificationSettings { setting in
            //: switch setting.authorizationStatus {
            switch setting.authorizationStatus {
            //: case .notDetermined, .denied, .ephemeral:
            case .notDetermined, .denied, .ephemeral:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: print()
                print()

            //: case .provisional, .authorized:
            case .provisional, .authorized:
                //: if let dataModel = model.data {
                if let dataModel = model.data {
                    //: let content = UNMutableNotificationContent()
                    let content = UNMutableNotificationContent()
                    //: content.title = dataModel.nickname ?? ""
                    content.title = dataModel.nickname ?? ""
                    //: content.body = model.showText ?? ""
                    content.body = model.showText ?? ""
                    //: let identifier = dataModel.uid ?? "\(AppName)__LocalPush"
                    let identifier = dataModel.uid ?? "\(userClientData)" + (String(appFatalUrl.prefix(5)) + String(notiResponseData.prefix(6)))
                    //: content.userInfo = ["identifier": identifier]
                    content.userInfo = [String(bytes: dataSinceName.map{upMain(create: $0)}, encoding: .utf8)!: identifier]
                    //: content.sound = UNNotificationSound.default
                    content.sound = UNNotificationSound.default

                    //: let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    let time = Date(timeIntervalSinceNow: 1).timeIntervalSinceNow
                    //: let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    let trigger = UNTimeIntervalNotificationTrigger(timeInterval: time, repeats: false)
                    //: let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    let request = UNNotificationRequest(identifier: identifier, content: content, trigger: trigger)
                    //: UNUserNotificationCenter.current().add(request) { _ in
                    UNUserNotificationCenter.current().add(request) { _ in
                    }
                }

            //: @unknown default:
            @unknown default:
                //: print("本地推送通知 -- 用户未授权\(setting.authorizationStatus)")
                //: break
                break
            }
        }
    }

    /// 获取firebase token
    //: class func getFireBaseToken(tokenBlock: @escaping (_ str: String) -> Void) {
    class func appear(tokenBlock: @escaping (_ str: String) -> Void) {
        //: Messaging.messaging().token { token, _ in
        Messaging.messaging().token { token, _ in
            //: if let token = token {
            if let token = token {
                //: tokenBlock(token)
                tokenBlock(token)
                //: } else {
            } else {
                //: tokenBlock("")
                tokenBlock("")
            }
        }
    }

    /// 获取ISO国家代码
    //: class func getCountryISOCode() -> [String] {
    class func can() -> [String] {
        //: var tempArr: [String] = []
        var tempArr: [String] = []
        //: let info = CTTelephonyNetworkInfo()
        let info = CTTelephonyNetworkInfo()
        //: if let carrierDic = info.serviceSubscriberCellularProviders {
        if let carrierDic = info.serviceSubscriberCellularProviders {
            //: if !carrierDic.isEmpty {
            if !carrierDic.isEmpty {
                //: for carrier in carrierDic.values {
                for carrier in carrierDic.values {
                    //: if let iso = carrier.isoCountryCode, !iso.isEmpty {
                    if let iso = carrier.isoCountryCode, !iso.isEmpty {
                        //: tempArr.append(iso)
                        tempArr.append(iso)
                    }
                }
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 获取用户代理状态
    //: class func getUsedProxyStatus() -> Bool {
    class func launch() -> Bool {
        //: if AppWebViewController.isUsedProxy() || AppWebViewController.isUsedVPN() {
        if ChangeRunningViewController.agent() || ChangeRunningViewController.response() {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedProxy() -> Bool {
    class func agent() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }

        //: if let httpProxy = dict["HTTPProxy"] as? String, !httpProxy.isEmpty { return true }
        if let httpProxy = dict[(String(show_sceneRevenueObserveId) + mainUpId.replacingOccurrences(of: "temp", with: "xy"))] as? String, !httpProxy.isEmpty { return true }
        //: if let httpsProxy = dict["HTTPSProxy"] as? String, !httpsProxy.isEmpty { return true }
        if let httpsProxy = dict[(main_canStr.replacingOccurrences(of: "revenue", with: "H") + show_sizeStr.replacingOccurrences(of: "actually", with: "ro"))] as? String, !httpsProxy.isEmpty { return true }
        //: if let socksProxy = dict["SOCKSProxy"] as? String, !socksProxy.isEmpty { return true }
        if let socksProxy = dict[(userPoorId.uppercased() + String(main_pathName.prefix(5)))] as? String, !socksProxy.isEmpty { return true }

        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: class func isUsedVPN() -> Bool {
    class func response() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(show_numberPath))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((constStopMsg.replacingOccurrences(of: "center", with: "a"))) || keyStr.contains((String(notiProductionNowWindowPath))) || keyStr.contains((notiWhiteMessage.replacingOccurrences(of: "feedback", with: "e"))) || keyStr.contains((String(app_confirmClickKey))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }

    /// 请求应用内评分 - iOS 13+ 优化版本
    //: class func requestInAppRating() {
    class func currencySource() {
        //: if #available(iOS 14.0, *) {
        if #available(iOS 14.0, *) {
            // iOS 14+ 使用新的 WindowScene API（推荐）
            //: if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
            if let windowScene = UIApplication.shared.connectedScenes.first as? UIWindowScene {
                //: SKStoreReviewController.requestReview(in: windowScene)
                SKStoreReviewController.requestReview(in: windowScene)
            }
            //: } else {
        } else {
            // iOS 13.x 使用传统 API
            //: SKStoreReviewController.requestReview()
            SKStoreReviewController.requestReview()
        }
    }

    func showAlert() {
        InsideUnknownProgressHUD.systemOfRules((String(noti_statusText.prefix(4)) + "y Aft" + String(show_presentationAdjustPendingText.prefix(8)) + String(appMakeKey.prefix(4))) + "\"" + (String(app_sceneId.suffix(8))) + "\"" + (String(main_firstUrl) + String(notiTransactionValue)))
    }
    
    // MARK: - Event

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: func applePay(productId: String, source: Int = -1, payType: ApplePayType, completion: ((Bool) -> Void)? = nil) {
    func businessLine(productId: String, source: Int = -1, payType: FormType, completion: ((Bool) -> Void)? = nil) {
        //: ProgressHUD.show()
        InsideUnknownProgressHUD.represent()
        //: var index = 0
        var index = 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, _, _ in
        HarvestMoonRequestDelegate.shared.window(productId: productId, payType: payType, source: index) { status, _, _ in
            //: ProgressHUD.dismiss()
            InsideUnknownProgressHUD.rating()
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: var isSuccess = false
                var isSuccess = false
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    self.showAlert()
                    
                //: case .veritySucceed, .renewSucceed:
                case .veritySucceed, .renewSucceed:
                    //: isSuccess = true
                    isSuccess = true
                    //: self.third_jsEvent_refreshCoin()
                    self.pullUp()

                //: default:
                default:
                    //: print(" apple支付充值失败：\(status.rawValue)")
                    //: break
                    break
                }
                //: completion?(isSuccess)
                completion?(isSuccess)
            }
        }
    }
}
